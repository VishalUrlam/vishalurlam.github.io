# Company logos

Drop company/institution logo images here (SVG or PNG, ideally transparent background).
Import them in `src/Companies.jsx` and set as the `logo` field, e.g.:

```js
import yc from './assets/company-assets/ycombinator.svg'
// ...
{ name: \"Y Combinator\", logo: yc },
```
